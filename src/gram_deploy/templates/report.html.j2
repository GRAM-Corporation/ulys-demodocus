<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Deployment Report: {{ report.deployment.location }}</title>
    <style>
        :root {
            --primary-color: #4285F4;
            --text-color: #333;
            --text-muted: #666;
            --border-color: #ddd;
            --bg-light: #f5f5f5;
            --bg-hover: #f9f9f9;
            --warning-bg: #fff3cd;
            --warning-border: #ffc107;
        }

        * { box-sizing: border-box; }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            max-width: 1000px;
            margin: 0 auto;
            padding: 20px 40px;
            line-height: 1.6;
            color: var(--text-color);
        }

        h1 {
            color: var(--text-color);
            border-bottom: 3px solid var(--primary-color);
            padding-bottom: 15px;
            margin-bottom: 20px;
        }

        h2 {
            color: #444;
            margin-top: 40px;
            padding-bottom: 10px;
            border-bottom: 1px solid var(--border-color);
        }

        h3 { color: var(--text-muted); }

        table {
            border-collapse: collapse;
            width: 100%;
            margin: 20px 0;
        }

        th, td {
            border: 1px solid var(--border-color);
            padding: 12px 15px;
            text-align: left;
        }

        th {
            background: var(--bg-light);
            font-weight: 600;
        }

        tr:hover { background: var(--bg-hover); }

        hr {
            border: none;
            border-top: 1px solid var(--border-color);
            margin: 40px 0;
        }

        .header-meta {
            color: var(--text-muted);
            margin-bottom: 30px;
        }

        .metrics {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
            gap: 20px;
            margin: 25px 0;
        }

        .metric {
            background: var(--bg-light);
            padding: 25px;
            border-radius: 10px;
            text-align: center;
        }

        .metric-value {
            font-size: 2.2em;
            font-weight: bold;
            color: var(--primary-color);
        }

        .metric-label {
            color: var(--text-muted);
            margin-top: 5px;
        }

        .issue {
            background: var(--warning-bg);
            border-left: 4px solid var(--warning-border);
            padding: 15px 20px;
            margin: 15px 0;
            border-radius: 0 8px 8px 0;
        }

        .issue h3 {
            margin-top: 0;
            color: #856404;
        }

        .insight {
            background: #e8f4fd;
            border-left: 4px solid var(--primary-color);
            padding: 15px 20px;
            margin: 15px 0;
            border-radius: 0 8px 8px 0;
        }

        .insight h3 {
            margin-top: 0;
            color: var(--primary-color);
        }

        .transcript {
            background: var(--bg-light);
            padding: 20px 25px;
            border-radius: 8px;
            max-height: 600px;
            overflow-y: auto;
        }

        .utterance {
            margin: 10px 0;
            line-height: 1.5;
        }

        .timestamp {
            color: var(--text-muted);
            font-family: 'Menlo', 'Monaco', monospace;
            font-size: 0.9em;
        }

        .speaker {
            font-weight: 600;
            color: var(--primary-color);
        }

        .empty-state {
            font-style: italic;
            color: var(--text-muted);
            padding: 20px;
            text-align: center;
        }

        .footer {
            text-align: center;
            color: var(--text-muted);
            font-size: 0.9em;
            margin-top: 50px;
            padding-top: 20px;
            border-top: 1px solid var(--border-color);
        }

        .summary-box {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 25px 30px;
            border-radius: 12px;
            margin: 20px 0;
        }

        .summary-box p {
            margin: 0;
            font-size: 1.1em;
            line-height: 1.7;
        }

        @media print {
            body { padding: 0; max-width: none; }
            .transcript { max-height: none; }
            h2 { page-break-before: auto; }
            .issue, .metric, .insight { break-inside: avoid; }
            .summary-box { background: var(--bg-light); color: var(--text-color); }
        }
    </style>
</head>
<body>
    <h1>Deployment Report: {{ report.deployment.location }}</h1>
    <p class="header-meta">
        <strong>Date:</strong> {{ report.deployment.date }} |
        <strong>Generated:</strong> {{ report.generated_at.strftime('%Y-%m-%d %H:%M UTC') }}
    </p>

    <h2>Executive Summary</h2>
    <div class="summary-box">
        <p>{{ report.executive_summary }}</p>
    </div>

    <h2>Deployment Metrics</h2>
    <div class="metrics">
        <div class="metric">
            <div class="metric-value">{{ report.metrics.get('duration_formatted', 'N/A') }}</div>
            <div class="metric-label">Duration</div>
        </div>
        <div class="metric">
            <div class="metric-value">{{ report.team_members | length }}</div>
            <div class="metric-label">Team Members</div>
        </div>
        <div class="metric">
            <div class="metric-value">{{ report.metrics.get('source_count', 0) }}</div>
            <div class="metric-label">Video Sources</div>
        </div>
        <div class="metric">
            <div class="metric-value">{{ report.action_items | length }}</div>
            <div class="metric-label">Action Items</div>
        </div>
    </div>

    <h2>Team Members</h2>
    <p>{{ report.team_members | join(', ') if report.team_members else 'N/A' }}</p>

    <h2>Key Events</h2>
    {% if report.key_events %}
    <table>
        <thead>
            <tr><th>Time</th><th>Type</th><th>Event</th><th>Description</th></tr>
        </thead>
        <tbody>
            {% for event in report.key_events[:20] %}
            <tr>
                <td>{{ format_time(event.canonical_time_ms) }}</td>
                <td>{{ event.event_type | enum_value }}</td>
                <td>{{ event.title }}</td>
                <td>{{ (event.description or '-') | replace('\n', ' ') | truncate(100) }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
    {% else %}
    <p class="empty-state">No key events recorded.</p>
    {% endif %}

    <h2>Issues Encountered</h2>
    {% if report.issues %}
    {% for issue in report.issues %}
    <div class="issue">
        <h3>{{ issue.title }}</h3>
        <p>
            <strong>Time:</strong> {{ format_time(issue.canonical_time_ms) }} |
            <strong>Severity:</strong> {{ issue.severity | format_severity }}
        </p>
        <p>{{ issue.description or 'No description' }}</p>
    </div>
    {% endfor %}
    {% else %}
    <p class="empty-state">No issues recorded.</p>
    {% endif %}

    <h2>Action Items</h2>
    {% if report.action_items %}
    <table>
        <thead>
            <tr><th>#</th><th>Action</th><th>Mentioned By</th><th>Assigned To</th><th>Priority</th></tr>
        </thead>
        <tbody>
            {% for item in report.action_items %}
            {% set mentioned = report.people_names.get(item.mentioned_by, item.mentioned_by) if item.mentioned_by else '-' %}
            {% set assigned = report.people_names.get(item.assigned_to, item.assigned_to) if item.assigned_to else '-' %}
            <tr>
                <td>{{ loop.index }}</td>
                <td>{{ item.description }}</td>
                <td>{{ mentioned }}</td>
                <td>{{ assigned }}</td>
                <td>{{ item.priority | format_priority }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
    {% else %}
    <p class="empty-state">No action items recorded.</p>
    {% endif %}

    <h2>Insights & Observations</h2>
    {% if report.insights %}
    {% for insight in report.insights %}
    <div class="insight">
        <h3>{{ insight.insight_type | enum_value | replace('_', ' ') | title }}</h3>
        <p>{{ insight.content }}</p>
    </div>
    {% endfor %}
    {% else %}
    <p class="empty-state">No insights recorded.</p>
    {% endif %}

    <h2>Full Transcript</h2>
    {% if report.utterances %}
    <div class="transcript">
        {% for utterance in report.utterances %}
        {% set speaker = report.people_names.get(utterance.speaker_id, utterance.speaker_id or 'Unknown') %}
        <p class="utterance">
            <span class="timestamp">[{{ format_time(utterance.canonical_start_ms) }}]</span>
            <span class="speaker">{{ speaker }}:</span>
            {{ utterance.text }}
        </p>
        {% endfor %}
    </div>
    {% else %}
    <p class="empty-state">No transcript available.</p>
    {% endif %}

    <div class="footer">
        <p>Report generated by GRAM Deployment Processing System</p>
    </div>
</body>
</html>
